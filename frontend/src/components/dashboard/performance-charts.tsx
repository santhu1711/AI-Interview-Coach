"use client";
import { Bar, BarChart, CartesianGrid, Legend, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import type { DashboardPerformance } from "@/types/dashboard";
const tooltipStyle = { background: "#18181b", border: "1px solid #3f3f46", borderRadius: "8px" };
export function PerformanceCharts({ performance }: { performance: DashboardPerformance }) { if (!performance.scoreTrend.length && !performance.domainPerformance.length && !performance.categoryComparison.length) return <div className="rounded-xl border border-dashed border-zinc-700 p-10 text-center text-zinc-400">Complete interviews and generate reports to unlock performance charts.</div>; const trend = performance.scoreTrend.map(point => ({ ...point, date: new Date(point.generatedAt).toLocaleDateString() })); const categories = performance.categoryComparison.map(item => ({ ...item, name: item.fieldCategory === "IT" ? "IT Field" : "Non-IT Field", averageScore: item.averageScore ?? 0 })); return <div className="grid gap-6 lg:grid-cols-2"><Chart title="Score trend"><ResponsiveContainer height="100%" width="100%"><LineChart data={trend}><CartesianGrid stroke="#27272a"/><XAxis dataKey="date" stroke="#a1a1aa"/><YAxis domain={[0, 100]} stroke="#a1a1aa"/><Tooltip contentStyle={tooltipStyle}/><Line dataKey="score" name="Score" stroke="#3b82f6" strokeWidth={3}/></LineChart></ResponsiveContainer></Chart><Chart title="Domain performance"><ResponsiveContainer height="100%" width="100%"><BarChart data={performance.domainPerformance}><CartesianGrid stroke="#27272a"/><XAxis dataKey="domain" stroke="#a1a1aa"/><YAxis domain={[0, 100]} stroke="#a1a1aa"/><Tooltip contentStyle={tooltipStyle}/><Bar dataKey="averageScore" fill="#22c55e" name="Average score"/></BarChart></ResponsiveContainer></Chart><Chart title="IT vs Non-IT"><ResponsiveContainer height="100%" width="100%"><BarChart data={categories}><CartesianGrid stroke="#27272a"/><XAxis dataKey="name" stroke="#a1a1aa"/><YAxis domain={[0, 100]} stroke="#a1a1aa"/><Tooltip contentStyle={tooltipStyle}/><Legend/><Bar dataKey="averageScore" fill="#3b82f6" name="Average score"/></BarChart></ResponsiveContainer></Chart></div>; }
function Chart({ title, children }: { title: string; children: React.ReactNode }) { return <section className="h-80 rounded-xl border border-zinc-800 bg-zinc-950 p-5"><h3 className="mb-5 font-semibold">{title}</h3><div className="h-60">{children}</div></section>; }
