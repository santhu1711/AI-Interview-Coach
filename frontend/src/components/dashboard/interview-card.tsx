import Link from "next/link";
import { optionLabel, statusLabel } from "@/lib/interview-labels";
import type { InterviewSummary } from "@/types/dashboard";
export function interviewAction(interview: InterviewSummary) { if (interview.status === "IN_PROGRESS" || interview.status === "CREATED") return { href: `/interview/${interview.id}`, label: "Continue" }; if (interview.status === "COMPLETED" || interview.status === "REPORT_GENERATED") return { href: `/report/${interview.id}`, label: interview.status === "COMPLETED" ? "Generate report" : "View report" }; return null; }
export function InterviewCard({ interview, actions }: { interview: InterviewSummary; actions?: React.ReactNode }) { const action = interviewAction(interview); return <article className="rounded-xl border border-zinc-800 bg-zinc-950 p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="flex flex-wrap gap-2 text-xs text-zinc-400"><span>{interview.fieldCategory === "IT" ? "IT Field" : "Non-IT Field"}</span><span>·</span><span>{interview.customDomain || optionLabel([], interview.interviewDomain)}</span><span>·</span><span>{statusLabel(interview.status)}</span></div><h3 className="mt-2 text-lg font-semibold">{interview.topic}</h3><p className="mt-1 text-sm text-zinc-400">{interview.targetRole} · {optionLabel([], interview.difficulty)}</p></div>{interview.overallScore !== null && <span className={`text-2xl font-bold ${interview.overallScore >= 70 ? "text-green-400" : interview.overallScore >= 50 ? "text-yellow-400" : "text-red-400"}`}>{interview.overallScore}</span>}</div><div className="mt-4 flex flex-wrap items-center justify-between gap-3"><time className="text-xs text-zinc-500" dateTime={interview.createdAt}>{new Date(interview.createdAt).toLocaleDateString()}</time><div className="flex gap-3">{action && <Link className="text-sm font-semibold text-blue-400 hover:text-blue-300" href={action.href}>{action.label}</Link>}{actions}</div></div></article>; }
