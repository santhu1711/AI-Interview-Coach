spring:
  datasource:
    url: jdbc:h2:mem:coach;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
app:
  jwt:
    secret: test-only-jwt-secret-that-is-at-least-thirty-two-bytes-long
  ai:
    provider: deterministic-test

